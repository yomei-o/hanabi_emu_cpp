# resume.md — 次に触るときの覚書

最終更新: 2026-08-13 (その2)

## いまどうなっているか

打ち上げ花火のシミュレータ。**星を1個ずつ3Dで積分**していて、菊・牡丹・柳・蜂・渦蜂・千輪・型物(ハート/輪/土星)と
スターマイン（100連打＋余韻、テーマ10種）まで動いている。計算も描画もすべて C++ → WASM。

- GitHub: https://github.com/yomei-o/hanabi_emu_cpp （main に push 済み）
- デモ: https://yomei-o.github.io/hanabi_emu_cpp/wasmdist/hanabi_os/ （Pages は main のルート配信）

## すぐ動かす

```sh
cd ~/test/hanabi_emu_cpp

# WASM ビルド（新しい emsdk は emcc だと C++ シンボルが解決できないので em++）
EMSDK=~/emsdk ./build.sh hanabi_os

# ブラウザで見る
cd wasmdist && python3 -m http.server 8765
# -> http://localhost:8765/hanabi_os/

# ネイティブ自己テスト（PNG を書き出す。目視確認はこれが速い）
clang++ -O3 -std=c++17 -I. src/hanabi_os.cpp -o /tmp/hanabi   # g++ 不可(C99 指定初期化子)
/tmp/hanabi 620 /tmp/climax.png 100    # 引数: フレーム数 出力PNG [連発数] [玉の種類]
/tmp/hanabi 500 /tmp/hachi.png 0 4     # 玉の種類: -1=おまかせ 0菊 1牡丹 2柳 3蜂 4渦蜂 5千輪 6♡ 7輪 8土星
/tmp/hanabi 640 /tmp/sakura.png 100 -1 2   # 6番目 = テーマ(0おまかせ 1菊 2桜 3銀 4牡丹 5柳 6蜂 7渦蜂 8千輪 9型物)
```

## ファイル

| ファイル | 中身 |
|---|---|
| `src/hanabi_os.cpp` | 物理・描画すべて（これ1本。約660行） |
| `build.sh` | emcc ラッパ。`EXPORTED_FUNCTIONS` に `_sim_get` を追加してある |
| `wasmdist/hanabi_os/index.html` | JSハーネス（universe_cpp のものにボタン状態表示を追加） |
| `olive.c` / `stb_image_write.h` | 描画（テキストのみ）/ ネイティブPNG出力 |
| `preview_*.png` | README 用（5号／クライマックス／蜂／千輪／ハート／土星） |

## 設計の要点（ここを崩さない）

**主要な計算はすべて逐次差分式。解析解も逆算ソルバも使わない。**
入力は火薬が与える初速と導火線の秒時だけで、**到達高度も開花直径も積分の結果**として出る。
（以前は「到達高度から初速を解析解で逆算」「開花直径から星初速を二分法で逆算」していたが廃止した）

菊の形を作っている物理：

1. 星は表面燃焼で半径 r が線形に減る → `k = ½ρCdA/m = 0.8636/(4 ρs r)` が **r に反比例して増大**。
   小さくなるほど急減速する。これが「勢いよく開いて、止まって、垂れる」形の正体。
2. 尾（引先）は残光ではなく、**星から剥がれる燃えかすを実粒子として放出**している。
   半径0.3mm の粒は k≈0.3 で放出直後にほぼ空気に対して静止し、その場で燃えて重力で垂れる。
3. 輝度は燃焼表面積 `(r/r₀)²` に比例 → 消え際が自然に暗くなる。
4. 連打が本物っぽく見えるのは**導火線の秒時をばらつかせて開発高度を揃えていない**から（`fuseJit`）。

玉種は物理量を変えるだけ（`burst()` の `tv/tb/tshed/tthrust`）。`enum { T_KIKU, T_BOTAN, T_YANAGI,
T_HACHI, T_UZU, T_SENRIN, T_HEART, T_RING, T_SATURN }`。
- 牡丹=剥離0、柳=初速×0.48・燃焼×1.95
- **蜂**=星が推力を出し、その向きが軸まわりに回る（`thrust/spin/sphase` と基底 `u,w`）。
  ω が速すぎると打ち消し合ってブルブルするだけ。**0.7〜6 rad/s** が正解。星数は×0.38に減らす
- **渦蜂**=蜂とは回り方の仕組みが違う。**星そのものが中心のまわりを周回**する(`Star::orbit`)。
  `x,y,z` は回る中心＝普通の弾道で、表示位置と火の粉は `hachi_offset()` が返すオフセット/接線速度を足す。
  推力が向心力なので **R = 推力/ω²**、回転は `dω/dt = -B|ω|ω` で落ちる。
  点火直後 42〜68 rad/s(7〜11回転/秒) → 輪が小さい → 回転が落ちて輪が開く。燃え尽きるまでに3〜4周巻く。
  **回転が速いと1サブステップで位相が飛ぶので、火の粉は円弧上に位相補間して撒く**
  (`hachi_offset_at()`)。これをやらないと軌跡が点線になる。
  燃焼は ×0.34(≒1.6秒)と短命。回転軸・向き・速さは玉全体で共有。
  （試行錯誤: 最初は推力を加速度に足すモデルで40m級の巨大な風車 → 「大きすぎる」→
   速く小さくしたが → 「星の中心が回転するイメージ」「最初速く回って落ちる」「光る時間は短い」→
   さらに「もっともっと回転が速い」で ω0 を上げ、見える周回数を稼ぐため推力を 900〜1500 に上げた。
   R=推力/ω² なので、ω を上げるだけだと輪が見えなくなる点に注意）
- **千輪**=`burst()` で星ではなく `spawn_child()` で子玉 Shell を撒く。`gen` で世代を区別。
  子玉は下向きにも撒かれるので、親用の「落下し始めたら開く」フォールバックを子玉には適用しないこと（`gen==0` 条件）
- **型物**=`shape_point()` の輪郭に並べ、中心からの距離に比例した速度で押し出す。土星だけ芯を3D球にして銀色に
- **錦冠(金冠)**=柳の強化版。初速×0.40・燃焼×2.30(≒10秒)・剥離×2.20。`chg=0.90` `c1=紅` で菊先紅。
  **一番重い玉**。1発あたり火の粉59,000粒(菊は11,000)。ただし `qual` が上限を押さえるので
  100連発でも 6.0ms/フレーム(菊は5.9ms)で、**払うのは速度ではなく密度**(qual 0.23 vs 菊0.52)。
  フル密度で出せるのは同時9〜10発ぶん。予算を120万にすると qual 0.46 / 9.2ms
- 芯は `Star::delay` で暗飛行（飛ぶが光らない）→ 時差開発。`crackle` で消え際に分砲

**スターマインの余韻**（`barPhase`: 0=通常 1=連打中 2=余韻）。`barLeft` が 0 になるのは最後の玉を
「打ち上げた」瞬間で、そこから開発まで約6秒ある。単純に連打終了で単発を再開すると**締めの大玉が開く前に
次が上がってしまう**ので、phase 2 では `shells` と `stars` が両方空になるまで待ち、そこから静寂の秒数
（`p_quiet`、既定6秒）を差分で消化してから通常に戻す。30発なら 打上4.5s → 最後の星が消えるのが18.0s →
静寂6s → 24.0s に復帰 → `launchTimer` ぶん待って 26.5s に単発再開（ネイティブのタイムラインで確認済み）。

**フルスクリーン鑑賞モード**。`sim_set(13,0)` で C++ 側の HUD 文字を消し（`sim_render` の先頭で早期 return）、
JS 側は `body.fs` クラスでページのUIを隠して canvas を `object-fit:contain` で全画面に。F キー / Esc で切替。
ネイティブでも8番目の引数に 0 を渡せば文字なしで撮れる（README の画像はこれで作った）。

**自動スターマイン**（`p_starInt`、既定120s）。`starTimer` は **barPhase==0 のときだけ**減る。
連打中と余韻では止まるので、間隔は「静かな時間」で測られる。ネイティブで7番目の引数に間隔を渡すと検証できる
（`/tmp/hanabi 4200 out.png 0 -1 0 15` で15秒おきに自動発火するのを確認済み）。

## 主なパラメータの在り処（`src/hanabi_os.cpp`）

- `spec_of(go)` — 号数から玉の諸元（打揚初速・秒時・星初速・星半径・燃焼時間・星数）
- `burst()` — 開発。フィボナッチ球で星を配る。芯（層）もここ
- `launch_ex(nx, go, type, fuseJit)` — 1発上げる
- `sim_step()` 内 — スターマインのスケジューラ、負荷に応じた `qual` の一次遅れ調整
- `sim_render()` — HDR加算バッファへのスプラット → `x/(1+x)` トーンマップ → 夜空に合成
- `MAX_SPARKS = 560000` / `MAX_STARS = 120000` — 粒子予算
- ABI: `sim_init/w/h/reset/step/render/click/set/action` + 独自追加の `sim_get(id)`
  （`sim_set` の id: 0号数 1星数 2打上間隔 3風速 4火の粉 5芯 6残光 7連打発数 8連打間隔 9余韻 10玉の種類 11テーマ 12自動スターマイン間隔 13HUD表示）
  （`sim_get` の id: 0連打残 1連打総数 2星数 3火の粉数 4玉数 5自動連発 6実測高度 7実測直径 8qual 9時刻 10フェーズ 11余韻残 12テーマ 13実テーマ 14次の自動連打まで 15自動間隔）
- `Pending` / `schedule_launch()` — **予約発射**。`t -= dt` で焼けて 0 で上がる。
  フェニックスで「大玉と小玉を空で同時に開かせる」ために使う（号数が違えば秒時も違うので、
  差のぶんだけ小玉を遅らせて撃つ）。余韻の判定にも `pending.empty()` を入れてある
- `phoenix_wave()` — 大玉4〜6発を横一列(±0.98)＋小玉13〜19発を秒時0.45倍で低く。
  テーマ11。号数スライダ10(尺玉)で長岡の写真に近くなる
- `THEMES[]` — スターマインのテーマ表 `{type, fixc, layout}`。layout 1=フェニックス。
  type -2=型物ランダム。添字0=おまかせで、
  `start_barrage()` が **連打ごとに1〜9からランダムに1つ引いて `curTheme` に確定**させる
  （全種類を混ぜると濁るので混成はやらない）。
  fixc は `FIXCOL[]` の添字で炎色を固定（桜＝ピンクの菊）。**JS 側の `THEMES` 配列と並びを合わせること**

## 性能

ネイティブ実測で 620フレーム(≒10秒)を 2.5秒 = 平均4ms/フレーム（100連打・火の粉42万粒）。
負荷が上がると `qual`（HUDの「引先%」）が自動で下がって密度が落ちる。
JS側は経過時間ぶん物理を進める（最大3ステップ/フレーム）ので fps が落ちてもスローモーションにならない。

## 今後の拡張アイデア

難易度は S(小: 1〜2時間) / M(半日) / L(1日以上) の目安。★ は効果が大きいと思うもの。

### A. 花火の種類を増やす

| # | 機能 | 難易度 | メモ |
|---|---|---|---|
| A1 ✅ | ~~千輪~~ (実装済) | — | 親玉が開くと同時に多数の小玉が飛び、少し遅れて一斉に小さく開く。`Shell` に「親から生まれる」経路と短い秒時を足すだけ。今の `burst()` から `launch_ex` を呼ばず直接 `Shell` を生成する形にする |
| A2 ✅ | ~~芯の時差開発~~ (実装済) | — | いまは全層が同時。層ごとに `delay` を持たせて 0.15〜0.4秒ずらすだけで、あの「開いてから中が光る」感じが出る |
| A3 ✅ | ~~錦冠（金冠）~~ (実装済) | — | 柳の強化版。燃焼10秒級・低速・剥離最大で地面近くまで垂れる。**消え際に色が変わる**（菊先紅＝消える直前だけ赤）のが肝。`chg` を 0.9 にして `c1` を赤にすれば出る |
| A4 ✅ | ~~型物(ハート・輪・土星)~~ (実装済。スマイルは未) | — | 星の配置を球ではなく「観客側を向いた平面上の曲線」にする。`burst()` のフィボナッチ球を差し替えるだけ。実物と同じく**横から見ると崩れる**のが正しい |
| A5 ✅ | ~~蜂~~ / ~~分砲~~ (実装済)、**落下傘**は未 | S | 落下傘＝抗力係数が途中で跳ね上がる。`Star` に「ある時刻で k を n 倍」を足すだけ |
| A6 | **ナイアガラ・仕掛け花火** | M | 地上の線分から火の粉を垂らす。粒子系はそのまま使える |
| A7 | **水中花火＋水面反射** | L | 半球状に開く玉＋川面。反射は「地平線より下に上下反転してもう一度スプラット、輝度を落として波で歪ませる」で十分それらしくなる |

### B. 物理をもっと本気にする

| # | 機能 | 難易度 | メモ |
|---|---|---|---|
| B1 ★ | **風の高度プロファイル（ウィンドシア）** | S | 上空ほど風が強い/向きが変わる。`wind(y)` にするだけ。**玉が流される・花が流れる**のは実際の花火大会でよく見る絵 |
| B2 ★ | **煙の蓄積** | M | 加算バッファとは別に減衰の遅い密度場を持ち、風で流す。連打すると本当に見えなくなる。奥の花火が煙で霞むと一気にリアルになる（散乱で減衰＋自発光を足す） |
| B3 | **玉込めの偏り** | S | 星の質量に系統的なばらつきを入れると花が歪む。今は ±3.5% の等方ばらつきだけ。実物の「真円が出ない」感じ |
| B4 | **炎色を輝線スペクトルから計算** | M | いま色は手打ち。Sr/Ba/Cu/Na の主輝線＋黒体連続光を CIE で合成して RGB にすると、**同じ緑でも Ba と Cu+Ba で違う**のが出る。火の粉の冷却も黒体軌跡に沿わせる |
| B5 | **侵食燃焼（erosive burning）** | S | 高速で飛ぶ星は燃焼が速まる。`dr` を速度依存に。開発直後だけ明るいのが再現できる |
| B6 | **玉皮の破片** | S | 開発時に非発光の破片を飛ばす。煙(B2)と合わせると開発の瞬間の質感が上がる |
| B7 | **不発・低開発** | S | 稀に秒時が狂って低い位置で開く／開かない。「本物っぽさ」はこういう所に出る |

### C. 見せ方・体験

| # | 機能 | 難易度 | メモ |
|---|---|---|---|
| C1 ★ | **音（遅延つき）** | M | 距離 D = 2.5×到達高度 なので 550m ≒ **1.6秒後にドン**。この視覚と音のズレが花火大会の実感そのもの。WebAudio で打上音・開発音・分砲のパチパチ。玉の大きさで低音の量を変える |
| C2 ★ | **番組（プログラム）編成** | M | 「単発の尺玉 → スターマイン → 大トリ」を時系列スクリプトで流す。実在の大会のプログラムを再現するのも面白い。JSON をパースするか C++ にタイムラインを持たせる |
| C3 | **長時間露光モード** | S | 残光の減衰を 1.0 にして加算し続ける＝花火写真。今の HDR バッファのままシャッター時間を持たせるだけ。撮れた絵は PNG 保存できるようにする |
| C4 | **カメラワーク** | M | 望遠でアップ／広角で全景／パン。いまの `setup_camera` は号数から一発で決めているので、目標画角に一次遅れで追従させる形にする（差分式のまま） |
| C5 | **玉の設計モード** | L | 「この形にするには星をどう詰めるか」を触れるようにする。星の直径・組成・詰め方を GUI で決めて上げてみる＝**花火職人シミュレータ**。このプロジェクトで一番面白くなる方向かもしれない |
| C6 | **観客視点の切り替え** | S | 桟敷席（近い・見上げる）／遠景（街ごし）。`camD` を変えるだけで印象が激変する |

### D. 性能

| # | 機能 | 難易度 | メモ |
|---|---|---|---|
| D1 | **WASM SIMD** | M | 粒子更新とバッファ減衰は素直にベクトル化できる。`-msimd128` で火の粉を数倍に |
| D2 | **スプラットのタイル分割＋pthread** | L | `-pthread` + SharedArrayBuffer（COOP/COEP ヘッダが要る＝Pages では面倒） |
| D3 | **粒子のSoA化** | M | いまは AoS。キャッシュ効率で効くはず |
| D4 | **火の粉のインパクト順ソート省略** | — | 加算合成なので順序不問。ここは既に最適 |

### 個人的なおすすめ順（次にやるなら）

1. **C1 音の遅延**（体験が別物になる。550m ≒ 1.6秒後にドン）
2. **B2 煙**＋**B1 風のプロファイル**（連打の説得力が段違い）
4. **C2 番組編成**（単発→スターマイン→大トリ。余韻が入ったので繋げるだけで大会になる）
5. **C5 玉の設計モード**（このプロジェクトの本命かも）

## 未確認

- ブラウザでの実 fps は未計測（ユーザ環境で確認してもらう段階で終了）
- Pages 反映後の動作確認は未実施
- 型物は正面（観客向き）固定。実物と同じく横から見ると崩れるはずだが、その見え方は未検証
